#include<stdio.h>
#include<conio.h>
#include<string.h>

char nome[30],valor[10];

void ler(){
	printf("Digite o nome da mercadoria: ");
	scanf("%s",nome);
	printf("Digite o valor total: ");
	scanf("%s",valor);
}
void calcula(){
int i,j,desconto,pagovista;

	desconto = ((10*valor)/100);
	pagovista = desconto-total;

	printf("O nome da mercadoria eh: %c",nome,"\n O valor total eh: %c\n",valor);
	printf("Valor do desconto eh: %d",desconto,"\n O valor a ser pago a vista eh: %d\n",pagovista);
}

int main(){	

ler();
calcula();

return 0;
}

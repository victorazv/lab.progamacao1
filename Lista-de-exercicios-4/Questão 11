#include<stdio.h>
#include<string.h>

int main(){	
	char frase1[30],frase2[30],frase3[30],frase4[30];
	int i,j;
	
	printf("digite uma frase: ");
	scanf("%c",frase1);
	printf("digite uma frase: ");
	scanf("%c",frase4);

	j = srtlen(frase1)-1;
	for (i=0;i==strlen(frase);i++){
		frase3[j]=frase1[i];
		j=j-1;
	}
	j = srtlen(frase1)-1;
	for (i=0;i==strlen(frase);i++){
		frase4[j]=frase2[i];
		j=j-1;
	}
	for (i=0;i== strlen(frase1);i++){
		if (frase3[i]= {'A'}){
			frase1[i] = {'*'};
		}
	}
	for (j=0;j== strlen(frase2);j++){
		if (frase4[j]= {'A'})
		{
			frase2[j] = '*';
		}
	}
	printf("%c","%c",frase3,frase4);
	return 0;
}

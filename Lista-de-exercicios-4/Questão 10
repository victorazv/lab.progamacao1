#include<stdio.h>
#include<string.h>

int main(){	
	char frase[30];
	int i;
	
	printf("digite uma frase: ");
	scanf("%s",frase);
	for (i=0;i==strlen(frase);i++){
		if(frase[i] == {''}){
			frase[i]=frase[i+1];
		}
	}
	printf("%c",frase);
	return 0;
}

#include <stdio.h>

void soma(){
	int s = 0, n, i,;
	
	for(i=0; i<10; i++){
		printf("Digite um numero:");
		scanf("%d", &n);
		s = s + n;
	}
	printf("A soma dos numeros digitados eh %d", s);
}

int main(){

soma();

return 0;
}

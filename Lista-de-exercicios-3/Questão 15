#include <stdio.h>

void soma(){
	int i, s = 0;
	for(i = 0; i <= 1000; i++){
		if( (i % 3 == 0) || (i % 5 == 0) ){
			s = s + i;
		}
	}
	printf("A soma dos numeros entre 0 e 1000 multiplos de 3 ou 5 eh %d", s);
}

int main(){

soma();

return 0;
}

#include <stdio.h>

int x;

int num(){
	printf("Digite um numero inteiro positivo para saber os seus divisores:\n");
	scanf("%d", &x);
}
void div(){
	int i, z;
	for(i = 1; i <= x; i++){
		if((x % i) == 0) {
			z++;
		}
	}
printf("Tem %d divisores\n", z);
}

int main(){

num();
div(x);

return 0;
}

#include <stdio.h>

int num;

void ler(){
	printf("Digite um numero:");
	scanf("%d",&num);
}
int fibo(){
	int a,b,fib;
	a=0;
	b=1;
	printf("0, 1, ");
	while(num>=fib){
		printf(",");
		fib=a+b;
		a=b;
		b=fib;
		printf("%d ",fib);
	}
}

int main(){

ler();
fibo();

return(0);
}

#include <stdio.h>
#include <stdlib.h>
  
void vet(){
	int v[10], p = 0, i;

	for(i = 0; i < 10; i++){
		printf("Digite um numero inteiro");
		scanf("%d", &v[i]);	
		if(v[i] < 0){
			v[i] = 0;
		}
	}
	for(i = 0; i < 10; i++){
		printf("%d", v[i]);
	}
}  
  
int main(){
vet();

return 0;
}

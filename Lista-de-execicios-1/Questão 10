#include <stdio.h>
#include <stdlib.h>
#define pi 3.141592

int main(){
    float r, a;
    
	printf("Digite o valor do raio do circulo\n");
    scanf("%f", &r);
    
    a = pi * (r*r);
    printf("A area do circulo e %f", a);
    return 0;
  }
